import React from "react";
import Button from "../StyledButton";

export default {
  title: "Button",
  component: Button,
};

export const Primary = () => (
  <Button bg="#0071f3" col="whitesmoke">
    Hello
  </Button>
);
